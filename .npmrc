# 启用 workspace 功能
shared-workspace-lockfile=true
# 允许从 workspace 引用依赖
link-workspace-packages=true
# 提升依赖到根目录
hoist=true
# 严格的 peer dependencies 处理
strict-peer-dependencies=false
# 自动安装 peer dependencies
auto-install-peers=true
